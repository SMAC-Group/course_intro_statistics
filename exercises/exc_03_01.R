fibonacci = function(x){
  # Base case
  if(___){
    stop("Incorrect input")
  }else if(___){
    return(0)
  }else if(___){
    return(1)
    # Recursive call
  }else{
    return(___)
  }
}

# Test output of the function
for(i in seq(10)){
  print(___)
}
