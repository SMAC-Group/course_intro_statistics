# Define function
find_gcd = function(a = NULL, b = NULL){
  #Compute the GCD of two positive defined integers
  if(___) stop("Please define two positive defined integers")
  if(___) stop("Please define two positive defined integers")
  while(___){
    if(___) res = a
    remaining = ___
    a = b
    b = ___
  }
  return(a)
}

# Test function
find_gcd(9045, 44840334375)
